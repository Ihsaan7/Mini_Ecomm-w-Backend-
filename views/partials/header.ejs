
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scatch - E-commerce</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@100;200;300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        'inter': ['Inter', 'sans-serif'],
                    }
                }
            }
        }
    </script>
</head>

<body class="font-inter bg-gray-50">
    <nav class="w-full flex justify-between fixed top-0 left-0 px-5 py-3 bg-white shadow-sm z-50">
        <h3 class="text-xl font-semibold text-gray-800">Scatch</h3>
        <% var loggedIn = typeof loggedIn !== "undefined" ? loggedIn : true %>
        <% if(loggedIn) { %>
            <div class="flex gap-5 items-center">
                <a href="/shop" class="text-gray-700 hover:text-blue-600 transition-colors">Shop</a>
                <a href="/cart" class="text-gray-700 hover:text-blue-600 transition-colors">Cart</a>
                <a class="text-red-600 hover:text-red-700 transition-colors" href="/logout">Logout</a>
            </div>
        <% } else { %>
            <div class="flex gap-5 items-center">
                <a href="/users/login" class="text-gray-700 hover:text-blue-600 transition-colors">Login</a>
                <a href="/" class="text-gray-700 hover:text-blue-600 transition-colors">Register</a>
            </div>
        <% } %>
    </nav>